Mock data is hand-authored from the values reported in issue #514, not captured
from a device.

The chassis is a plain Catalyst 9300, the family whose uplink module is
removable and therefore reported as its own FRU row.
