Mock data is hand-authored to exercise the claimed-slot guard, not captured
from a device.

The chassis is a plain Catalyst 9300 — the family that accepts a removable
`C9300-NM-*` — so the FRU uplink rows here describe hardware that can exist.
