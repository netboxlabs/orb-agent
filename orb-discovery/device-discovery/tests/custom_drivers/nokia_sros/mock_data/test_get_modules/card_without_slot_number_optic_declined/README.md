This mock data is hand-authored and not captured from a device.

The card element carries no slot-number, so row parsing drops it and the
chassis reads as having no card hierarchy. The element's presence in the reply
is what must decline the optic, since a chassis with a card is not a fixed-port
platform.
