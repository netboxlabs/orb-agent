This mock data is hand-authored and not captured from a device.

The chassis identity deliberately names no family in the allowlist, so this
pins the refusal itself. It does not name a real platform: a real fixed-port
family would belong on the allowlist rather than in this scenario.
