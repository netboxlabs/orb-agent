This mock data is hand-authored and not captured from a device.

The rack claims no card-shaped row, so the no-card clause would admit the
optic, and the row at the optic's parent path carries a blank PID field. The
inventory template yields an empty PID for that row while still reporting the
row itself, which is what must decline the promotion.
