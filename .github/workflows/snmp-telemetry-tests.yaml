name: SNMP Telemetry - test
on:
  workflow_dispatch:
  push:
    branches-ignore:
      - release
    paths:
      - "orb-telemetry/snmp-telemetry/**"
      - "!orb-telemetry/snmp-telemetry/docker/**"
  pull_request:
    paths:
      - "orb-telemetry/snmp-telemetry/**"
      - "!orb-telemetry/snmp-telemetry/docker/**"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    uses: ./.github/workflows/_backend-test-go.yaml
    with:
      app-name: snmp-telemetry
      parent-dir: orb-telemetry
